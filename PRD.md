# 号来了 PRD

## 1. 项目概述

“号来了”是一个自用型微信小程序，用于监控武汉同济医院官方小程序中的指定医生号源。当前目标为 2 个固定医生，系统持续监控未来 14 天滚动窗口内是否出现可预约号源。

一旦发现目标医生有号，系统需要在 10 秒内通过微信小程序订阅消息通知用户。项目只服务你和太太两位用户，不做公开推广，不做自动挂号，只做号源发现、提醒和历史变化分析。

## 2. 背景与问题

武汉同济医院热门医生号源紧张，放号时间可能不固定。手动反复打开医院官方小程序刷新，成本高、容易错过，也很难长期积累“什么时候更容易放号”的经验。

本项目希望解决三个问题：

- 自动监控：替代人工高频查看号源。
- 快速提醒：发现号源后尽快通知。
- 历史分析：保存号源变化，帮助判断大概放号时间。

关键原则：

- 不自动代替用户挂号。
- 不绕过验证码、风控或平台限制。
- 不做批量化、商业化、黄牛化能力。
- 尽量使用官方小程序的正常登录态与数据来源。
- 由于号源查询需要登录，需要谨慎处理登录凭证、会话有效期和隐私数据。

## 3. 已确认约束

- 目标医院：武汉同济医院。
- 数据来源：医院官方小程序。
- 目标医生：2 个固定医生：光谷院区产科林星光、光谷院区风湿免疫内科凃巍。
- 号源类型：专家号 · 副主任医师。
- 科室、院区、号源类型：固定。
- 日期策略：未来 14 天滚动窗口。
- 重点监控时段：16:50-17:10。
- 医院官方小程序账号：两位用户使用同一个账号。
- 查询条件：需要登录，无验证码。
- 通知渠道：仅微信小程序订阅消息。
- 通知延迟目标：10 秒内。
- 使用范围：你和太太两人自用。
- 历史数据：需要保存号源变化，用于分析放号时间。
- 当前基础设施：已获得微信小程序 AppID：`wx3d8692defee95f21`；已安装微信开发者工具；暂无独立后端服务器或域名。

## 4. 产品目标

### 4.1 MVP 目标

- 支持你和太太两个用户登录小程序。
- 支持配置 2 个固定医生的监控任务。
- 支持设置未来 7 天滚动窗口。
- 后台持续检查官方小程序号源。
- 发现号源后 10 秒内触发微信订阅消息。
- 保存每次检测结果和号源变化记录。
- 展示任务状态、最近检查时间、最近结果、通知记录和历史号源变化。

### 4.2 非目标

MVP 阶段不做：

- 自动挂号、自动提交订单或自动支付。
- 多医院、多科室泛化。
- 面向陌生用户开放注册。
- 多渠道通知。
- 绕过登录、验证码或风控。
- 大规模高频并发监控。

## 5. 用户与权限

### 5.1 用户范围

仅支持两个白名单用户：

- 用户 A：你本人。
- 用户 B：你太太。

可通过微信 openid 做白名单限制。非白名单用户打开小程序时，只展示无权限提示，不允许创建或查看监控任务。

### 5.2 用户权限

两位用户权限相同：

- 查看所有监控任务。
- 修改未来 7 天窗口配置。
- 暂停或恢复监控。
- 查看历史号源变化。
- 接收各自授权过的订阅消息。

## 6. 核心功能需求

### 6.1 首页 / 监控概览

首页展示 2 个医生的监控状态。

每个医生卡片展示：

- 医生姓名
- 医院：武汉同济医院
- 固定科室
- 固定院区
- 固定号源类型
- 监控窗口：未来 7 天
- 重点监控时段：16:50-17:10
- 当前状态：监控中 / 已暂停 / 发现有号 / 查询异常 / 登录失效
- 最近检查时间
- 最近一次结果
- 最近发现有号时间

核心操作：

- 暂停监控
- 恢复监控
- 查看详情
- 重新授权订阅消息

### 6.2 监控配置

由于医生、科室、院区、号源类型固定，MVP 不需要复杂的新建任务表单。建议采用“预置医生 + 简单配置”的方式。

可配置项：

- 未来 7 天滚动窗口。
- 每个医生是否启用监控。
- 每个医生的检查频率。
- 是否通知你。
- 是否通知太太。

后台固定配置项：

- 医生 ID 或医生名称。
- 科室 ID 或名称。
- 院区 ID 或名称。
- 号源类型。
- 官方小程序接口标识。
- 重点监控时段。

### 6.3 号源检测

系统持续检查 2 个医生在未来 7 天内是否存在可预约号源。

检测结果至少记录：

- 医生
- 日期
- 时段
- 号源类型
- 剩余号源数量，如可获取
- 挂号费，如可获取
- 是否可预约
- 检测时间
- 原始接口响应摘要
- 登录状态
- 错误信息

状态规则：

- 无号：记录本次检查，不通知。
- 有号：保存号源快照，判断是否为新增或变化，必要时通知。
- 登录失效：停止该轮检测，提示用户重新登录或更新凭证。
- 查询异常：记录异常，连续异常达到阈值后在小程序内显示告警。

### 6.4 检测频率与 10 秒目标

10 秒内通知是本项目最大的技术约束。项目明确采用“小程序 + 独立常驻后端服务”的架构，由后端常驻进程执行秒级轮询。

建议策略：

- 白天重点时段：每 5-10 秒检查一次。
- 非重点时段：每 30-60 秒检查一次。
- 夜间低概率时段：每 1-5 分钟检查一次。
- 发现疑似有号后：立刻复查 1 次，避免误报。

注意：

- 高频查询可能触发官方小程序接口风控。
- 10 秒目标需要独立常驻服务更可靠。
- 如果官方接口有访问限制，需要在稳定性和实时性之间取舍。

### 6.5 消息通知

通知渠道只使用微信小程序订阅消息。

通知触发条件：

- 某医生某日期某时段从无号变为有号。
- 新增可预约日期。
- 新增可预约时段。
- 剩余号源数量从 0 变为大于 0。

通知内容建议：

- 医生姓名
- 科室 / 院区
- 可预约日期
- 可预约时段
- 发现时间
- 点击进入任务详情

通知限制与应对：

- 微信订阅消息需要用户主动授权。
- 一次性订阅消息可能只能发送一次，需要用户定期重新授权。
- 对你和太太分别维护订阅授权状态。
- 小程序首页要明确显示“通知授权是否可用”。

### 6.6 历史号源变化

需要保存号源变化，用于分析放号时间。

历史记录包括：

- 首次发现时间
- 医生
- 日期
- 时段
- 号源类型
- 剩余数量
- 消失时间，如后续变为无号
- 持续时间
- 通知是否发送成功

分析视图：

- 按医生查看历史放号时间分布。
- 按星期几查看放号概率。
- 按一天内的时间段查看放号概率。
- 最近 7 / 30 / 90 天号源变化列表。

MVP 可以先做列表和简单统计，后续再做图表。

### 6.7 登录态管理

号源查询需要登录，但没有验证码。MVP 需要先验证官方小程序登录态能否被后端稳定复用。

可能方案：

- 使用用户在小程序内登录后的 session/token。
- 由后端保存必要的登录态信息。
- 登录失效时提示用户重新打开小程序完成登录。

安全要求：

- 只保存查询号源所必需的最小凭证。
- 凭证加密存储。
- 不在日志中打印完整 token、cookie、身份证、手机号等敏感信息。
- 支持手动清除登录凭证。

### 6.8 详情页

每个医生有一个详情页。

展示内容：

- 当前监控状态
- 未来 7 天内的最近一次检测结果
- 最近一次成功检测时间
- 最近一次失败原因
- 最近发现的号源
- 历史变化记录
- 通知记录
- 登录状态

操作：

- 暂停 / 恢复该医生监控
- 调整未来 7 天窗口
- 重新授权订阅消息
- 重新登录官方小程序账号

## 7. 数据模型草案

### 7.1 User

- id
- openid
- role
- displayName
- isWhitelisted
- subscribeStatus
- createdAt
- updatedAt

### 7.2 DoctorConfig

- id
- doctorName
- hospitalName
- campusName
- departmentName
- registrationType
- officialDoctorId
- officialDepartmentId
- enabled
- createdAt
- updatedAt

### 7.3 MonitorSetting

- id
- doctorConfigId
- windowDays
- checkIntervalSeconds
- daytimeIntervalSeconds
- nighttimeIntervalSeconds
- notifyUserIds
- status
- lastCheckedAt
- lastSuccessAt
- lastError
- createdAt
- updatedAt

### 7.4 LoginCredential

- id
- ownerUserId
- source
- encryptedCredential
- expiresAt
- status
- lastValidatedAt
- createdAt
- updatedAt

### 7.5 CheckLog

- id
- doctorConfigId
- checkedAt
- status
- hasAvailability
- responseHash
- errorCode
- errorMessage
- latencyMs

### 7.6 AvailabilitySnapshot

- id
- doctorConfigId
- availableDate
- session
- registrationType
- remainingCount
- fee
- status
- firstSeenAt
- lastSeenAt
- disappearedAt
- rawPayloadHash

### 7.7 NotificationRecord

- id
- userId
- doctorConfigId
- availabilitySnapshotId
- channel
- status
- errorMessage
- sentAt

## 8. 技术架构

### 8.1 架构决策

由于目标延迟是 10 秒内，项目明确采用：

- 微信小程序：配置、展示、订阅消息授权。
- 独立后端服务：常驻运行，负责秒级轮询、登录态管理、号源解析、通知触发。
- 数据库：保存配置、检测日志、号源变化、通知记录。
- 微信服务端接口：发送订阅消息。

本项目不使用微信云开发作为号源监控方案。号源监控由独立常驻后端服务完成，因为 10 秒内提醒需要秒级轮询，云函数定时触发更适合分钟级任务。

### 8.2 模块职责

微信小程序负责：

- 微信登录，获取用户 openid。
- 白名单用户鉴权。
- 展示 2 个医生的监控状态。
- 调整未来 7 天滚动窗口。
- 暂停或恢复监控。
- 发起订阅消息授权。
- 展示历史号源变化和通知记录。
- 提示登录态失效或通知授权失效。

独立常驻后端负责：

- 保存医生固定配置。
- MVP 当前先保存你的 openid 白名单；太太作为体验成员使用。后续如需接收通知，再补充太太 openid。
- 管理医院官方小程序登录态。
- 在 16:50-17:10 重点时段每 5-10 秒检查号源。
- 对比号源快照，识别新增号源。
- 保存检测日志、号源历史、通知记录。
- 调用微信服务端接口发送订阅消息。
- 暴露 HTTPS API 给小程序查询状态和修改配置。

数据库负责：

- 用户、医生配置、监控设置。
- 登录凭证密文。
- 检测日志。
- 号源快照。
- 通知记录。
- 简单统计分析所需的历史数据。

### 8.3 后端技术选型

MVP 默认采用 Node.js 独立后端服务。

默认选型：

- Web 框架：Fastify 或 Express。
- 任务调度：后端进程内轮询循环，后续可升级为 BullMQ / Redis。
- 数据库：SQLite 起步。
- 部署方式：云服务器 + systemd 常驻服务。

选择 Node.js 的原因：

- 更贴近微信小程序生态和服务端接口调用。
- 适合实现 HTTPS API、订阅消息发送、轮询调度和轻量后台。
- 项目规模较小，Node.js + SQLite 可以快速完成 MVP。

如果后续号源解析或统计分析明显复杂，再考虑引入 Python 辅助脚本，但不改变主架构。

### 8.4 部署选择

由于目前没有服务器，需要从零准备。推荐路线：

1. 先注册微信小程序账号。
2. 本地完成接口分析和最小监控脚本。
3. 购买一台轻量云服务器。
4. 部署常驻监控服务。
5. 接入微信小程序订阅消息。

云服务器建议：

- 地区：中国大陆，网络访问医院官方小程序接口更稳定。
- 配置：1 核 1G 或 2 核 2G 起步即可。
- 系统：Ubuntu LTS。
- 服务管理：systemd 或 Docker。

### 8.5 官方小程序接口分析

需要先完成一次技术验证：

- 打开武汉同济医院官方小程序。
- 登录账号。
- 进入 2 个医生的挂号页面。
- 观察号源查询请求。
- 确认请求参数、登录态、返回结构、有效期。
- 验证后端能否在不打开小程序 UI 的情况下复用登录态查询。

如果官方小程序接口无法稳定复用，需要调整方案，例如：

- 降低自动化程度。
- 增加人工更新登录态步骤。
- 改用更稳定的官方入口。

## 9. 从零配置步骤

### 9.1 注册微信小程序账号

1. 打开微信公众平台：https://mp.weixin.qq.com/
2. 点击注册，选择“小程序”。
3. 使用一个未绑定其他公众平台账号的邮箱注册。
4. 完成邮箱验证。
5. 选择主体类型。自用测试可先确认个人主体是否满足需要；如果订阅消息模板或接口能力受限，再考虑企业主体。
6. 完成管理员微信绑定。
7. 进入小程序后台，记录 AppID。

### 9.2 安装开发工具

1. 下载并安装微信开发者工具：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 使用管理员微信扫码登录。
3. 新建小程序项目，填入 AppID。
4. 选择不使用云服务。后端服务由独立云服务器提供。

### 9.3 配置服务器域名

小程序上线后，请求后端接口需要在后台配置合法域名。

步骤：

1. 购买并备案一个域名，或使用已有已备案域名。
2. 将域名解析到云服务器。
3. 配置 HTTPS 证书。
4. 在微信公众平台后台进入“开发管理 / 开发设置”。
5. 配置 request 合法域名，例如 `https://api.example.com`。

开发阶段可以在微信开发者工具中临时勾选“不校验合法域名”，但真机和线上版本需要正式配置。

### 9.4 开通订阅消息

1. 进入微信公众平台小程序后台。
2. 找到“功能 / 订阅消息”。
3. 选择合适的公共模板，例如预约提醒、服务提醒、状态提醒等，具体以后台可选模板为准。
4. 记录模板 ID。
5. 在小程序前端调用订阅授权 API，让你和太太分别授权。
6. 后端通过微信接口发送订阅消息。

注意：

- 模板字段必须与微信后台模板一致。
- 用户未授权或授权次数耗尽时，发送会失败。
- 小程序内需要显示授权状态，并提供重新授权入口。

### 9.5 准备云服务器

1. 购买轻量云服务器，例如腾讯云、阿里云或华为云。
2. 选择 Ubuntu LTS。
3. 配置安全组，只开放 SSH、HTTP、HTTPS。
4. 安装 Node.js 运行环境。
5. 安装数据库，MVP 可先使用 SQLite。
6. 使用 systemd 或 Docker 让监控服务常驻运行。
7. 配置日志轮转和异常重启。

### 9.6 配置微信服务端能力

后端需要：

- AppID
- AppSecret
- 订阅消息模板 ID
- 白名单用户 openid
- 后端服务域名

后端流程：

1. 使用 AppID / AppSecret 获取微信 access_token。
2. 保存并定期刷新 access_token。
3. 发现号源后调用订阅消息发送接口。
4. 记录发送结果。

### 9.7 上线前检查

- 两位用户都能登录小程序。
- 你的 openid 已在白名单中；太太已加入体验成员。
- 订阅消息授权成功。
- 后端能查询 2 个医生号源。
- 登录态失效时能正确提示。
- 发现模拟号源时能在 10 秒内发送通知。
- 检测日志、通知日志、历史变化都能写入数据库。

## 10. 关键流程

### 10.1 首次使用流程

1. 你注册并创建小程序。
2. 部署后端监控服务。
3. 在小程序中登录并完成订阅消息授权。
4. 太太作为体验成员打开小程序。
5. 后台保存你的 openid 到白名单。
6. 配置 2 个医生的固定监控信息。
7. 启动监控服务。

### 10.2 日常监控流程

1. 后端按照策略检查 2 个医生。
2. 查询未来 7 天滚动窗口内号源。
3. 保存检测日志。
4. 对比上一次快照。
5. 如发现新增号源，保存历史变化。
6. 向已授权用户发送微信订阅消息。
7. 用户点击通知进入小程序详情页。
8. 用户自行前往官方小程序完成挂号。

### 10.3 登录失效流程

1. 后端查询接口返回登录失效。
2. 标记监控状态为“登录失效”。
3. 小程序首页显示异常。
4. 用户重新登录或刷新凭证。
5. 后端验证凭证可用后恢复监控。

## 11. 成功指标

- 发现延迟 P95 小于 10 秒，前提是官方接口允许该频率。
- 订阅消息发送成功率大于 95%。
- 查询成功率大于 98%。
- 登录态失效率可观测、可恢复。
- 号源变化记录完整，能回答“最近什么时候放过号”。
- 误报率低于 5%。

## 12. 风险与应对

- 10 秒延迟风险：需要高频轮询，可能触发风控。应先做接口频率验证，并准备分时段频率策略。
- 登录态风险：官方小程序登录态可能短期失效。需要做失效提醒和手动刷新机制。
- 订阅消息限制：用户授权次数不足会导致无法通知。需要在首页显示授权状态。
- 接口变化风险：官方小程序接口字段可能变动。需要保留原始响应摘要和错误日志，便于快速修复。
- 合规风险：只做个人提醒，不自动挂号，不绕过验证，不对外开放。
- 基础设施缺失：需要先完成小程序账号、服务器、域名、HTTPS、订阅消息配置。

## 13. 仍待补充信息

1. 是否已有可用于备案的域名。
2. 服务器倾向使用腾讯云、阿里云还是其他云厂商。
3. 订阅消息模板 ID。
4. 你的 openid 白名单；太太后续如需通知再补 openid。

## 14. 下一步建议

1. 补齐两个医生的院区和号源类型配置。
2. 微信小程序账号已注册，AppID 为 `wx3d8692defee95f21`。
3. 用微信开发者工具导入 `miniapp` 目录。
4. 手动抓取或分析官方小程序的号源查询接口。
5. 本地写一个最小脚本，验证登录态能否查询号源。
6. 使用 Node.js 搭建独立常驻后端服务。
7. 购买云服务器并部署常驻监控服务。
8. 接入微信订阅消息，完成端到端模拟通知。
